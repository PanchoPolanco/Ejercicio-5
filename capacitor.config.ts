import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ernestoverdugo.guia5',
  appName: 'guia5',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
